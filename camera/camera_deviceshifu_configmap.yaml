apiVersion: v1
kind: ConfigMap
metadata:
  name: camera-configmap-0.0.1
  namespace: deviceshifu
data:
  driverProperties: |
    driverSku: hikvision 
    driverImage: 
  instructions: |
    instructions:
      cmd:
  telemetries: |
    telemetrySettings:
      telemetryUpdateIntervalInMiliseconds: 1000
